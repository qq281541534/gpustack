server {
    listen 80;
    server_name llm.lmzjai.com;

    location / {
        return 301 https://$server_name$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

server {
    listen 443 ssl;
    server_name llm.lmzjai.com;

    client_max_body_size 10G;

    location / {
        # 如果 Nginx 和 GPUStack 在同一台机器（宿主机 Nginx）：
        # proxy_pass http://127.0.0.1:8080;
        # 如果 Nginx 在 Docker 里，且 GPUStack 已加入同一 network：
        proxy_pass http://gpustack-server:80;
        include proxy.conf;
    }

    ssl_certificate /etc/ssl/llm.lmzjai.com.pem;
    ssl_certificate_key /etc/ssl/llm.lmzjai.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
}
