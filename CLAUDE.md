# GPUStack Claude Agent Loader

## AI Issue to Production

交付、PR、CI/CD、发布和回滚使用 `ai-issue-to-production`。
不能自动加载时读取公司级源仓库
`company-ai-skills/skills/ai-issue-to-production/SKILL.md`，只按当前任务需要读取 references。
项目事实和持续授权范围见本项目 `AGENTS.md`；普通文档按 skill 的轻量路径处理。
